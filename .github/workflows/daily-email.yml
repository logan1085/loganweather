name: Daily Email Trigger

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call daily notifications endpoint
        run: |
          curl -sS -X POST "${{ secrets.DAILY_TRIGGER_URL }}?force=1"
